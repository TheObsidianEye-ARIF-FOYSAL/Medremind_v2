# Server: runtime-generated logs/database, never commit BDApps traffic dumps
# or the SQLite user store.
server/*.txt
server/*.log
server/*.db

# BDApps credentials — never commit, see server/bdapps_config.example.php
server/bdapps_config.php/MedReminder/
